/**
 * excluded.js — works pulled from the site.
 *
 * A slug here is dropped from the landing folio AND gets no /works/ page.
 * The project folder under public/projects/ stays untouched, so bringing a
 * work back is deleting one line.
 */
export const EXCLUDED = [
  'journey-of-my-paper-plane',
  'friendly-stories',
  'how-to-think',
  'flatline',
  'wonderful-worlds',
]

export default EXCLUDED
